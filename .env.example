# Copy to .env (gitignored):  cp .env.example .env
# Loaded by the broker/db settings when services run from the repo root.
REDIS_URL=redis://:root@localhost:6379
DB_URL=postgresql+asyncpg://root:root@localhost:5432/saga
